name: check instance id
on:
  push:
    branches:
      - main
jobs:
  identify-runner:
    runs-on: ubuntu-latest
    steps:
      - name: hello world
        run: echo "hello world"

      # - name: Sleep for 10 seconds
      #   run: sleep 10
